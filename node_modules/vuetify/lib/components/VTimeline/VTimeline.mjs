// @ts-nocheck

/* eslint-disable */
// Styles
import "./VTimeline.sass"; // Types

import mixins from "../../util/mixins.mjs"; // Mixins

import Themeable from "../../mixins/themeable/index.mjs";
export default mixins(Themeable
/* @vue/component */
).extend({
  name: 'v-timeline',

  provide() {
    return {
      timeline: this
    };
  },

  props: {
    alignTop: Boolean,
    dense: Boolean,
    reverse: Boolean
  },
  computed: {
    classes() {
      return {
        'v-timeline--align-top': this.alignTop,
        'v-timeline--dense': this.dense,
        'v-timeline--reverse': this.reverse,
        ...this.themeClasses
      };
    }

  },

  render(h) {
    return h('div', {
      staticClass: 'v-timeline',
      class: this.classes
    }, this.$slots.default);
  }

});
//# sourceMappingURL=VTimeline.mjs.map