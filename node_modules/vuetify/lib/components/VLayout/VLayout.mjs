import { createVNode as _createVNode } from "vue";
// Styles
import "./VLayout.sass"; // Utilities

import { defineComponent } from 'vue';
import { makeProps, useRender } from "../../util/index.mjs"; // Composables

import { createLayout, makeLayoutProps } from "../../composables/layout.mjs";
export default defineComponent({
  name: 'VLayout',
  props: makeProps(makeLayoutProps()),

  setup(props, {
    slots
  }) {
    const {
      layoutClasses,
      getLayoutItem,
      items
    } = createLayout(props);
    useRender(() => {
      var _slots$default;

      return _createVNode("div", {
        "class": layoutClasses.value
      }, [(_slots$default = slots.default) == null ? void 0 : _slots$default.call(slots)], 2);
    });
    return {
      getLayoutItem,
      items
    };
  }

});
//# sourceMappingURL=VLayout.mjs.map